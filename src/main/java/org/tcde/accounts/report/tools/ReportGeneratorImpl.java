package org.tcde.accounts.report.tools;

import java.io.File;

public class ReportGeneratorImpl  implements ReportGenerator{

    @Override
    public ReportGenResult generateReports(File templateFile, File tmplValProviderFile, String outputFolderPath) {
        ReportGenResult result = new ReportGenResult();

        return result;
    }
}
